-- ============================================================
-- 채팅 이미지 첨부 — messages에 이미지 주소 칸 하나 추가
-- 여러 번 실행해도 안전합니다.
--
-- 이걸 실행하기 전에는 채팅 입력창의 '+' 버튼이 **숨겨져** 있습니다.
-- (없는 칸에 저장하려다 실패하는 버튼을 두지 않으려고 그렇게 만들었습니다)
-- 실행하면 자동으로 나타납니다.
-- ============================================================

alter table public.messages
  add column if not exists image_url text;
